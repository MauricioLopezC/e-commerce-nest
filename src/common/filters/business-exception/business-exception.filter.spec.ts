import { BusinessExceptionFilter } from './business-exception.filter';

describe('BusinessExceptionFilter', () => {
  it('should be defined', () => {
    expect(new BusinessExceptionFilter()).toBeDefined();
  });
});
